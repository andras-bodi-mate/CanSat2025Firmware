#include "position.hpp"
#include "position.hpp"

Position::Position(bool isValid,float latitude,float longitude,float altitude): isValid(isValid),latitude(latitude),longitude(longitude),altitude(altitude) {}
