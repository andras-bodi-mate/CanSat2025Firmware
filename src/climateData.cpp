#include "climateData.hpp"

ClimateData::ClimateData(bool isValid, float temperature, float pressure, float humidity, float altitude) : isValid(isValid), temperature(temperature), pressure(pressure), humidity(humidity), altitude(altitude) {}