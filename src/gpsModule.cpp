#include "gpsModule.hpp"

GpsModule::GpsModule(Pin rxPin, Pin txPin): rxPin(rxPin), txPin(txPin) {}

void GpsModule::begin() {
}

GpsData &GpsModule::read() {
    // TODO: insert return statement here
}
